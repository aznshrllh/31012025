export type TReedemReward = {
  name: string;
  imgUrl: string;
  point: number;
};

export type TPrivillages = {
  name: string;
  imgUrl: string;
  expire: string;
};

export type TLocation = {
  name: string;
  address: string;
  distance: number;
};
